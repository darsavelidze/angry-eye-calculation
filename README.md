# 👁️ Программа гимнастики для глаз с нейросетевым отслеживанием взгляда

Интерактивная программа для выполнения гимнастики глазодвигательных мышц с автоматическим контролем правильности выполнения упражнений. Программа использует нейросетевую модель **L2CS-Net** для определения направления взгляда по изображению с веб-камеры в реальном времени.

> **Проектная работа** для открытой городской научно-практической конференции «Инженеры будущего»  
> **Автор:** Данилова Арина, ученица 10Б класса, ГБОУ Школа № 924  
> **Руководитель:** Дарсавелидзе Александр Александрович, учитель информатики

---

## 📌 О проекте

Длительная работа за экраном компьютера приводит к повышенной зрительной нагрузке. Офтальмологи рекомендуют выполнять упражнения для глаз каждые 30–40 минут, однако существующие приложения для гимнастики глаз лишь показывают, куда нужно смотреть, но **не проверяют**, действительно ли пользователь выполняет упражнение.

Данный проект решает эту проблему: программа **отслеживает направление взгляда** с помощью нейронной сети и **фиксирует выполнение** каждого упражнения только тогда, когда пользователь действительно смотрит в нужном направлении.

### Ключевые особенности

- 🧠 **Нейросетевое отслеживание взгляда** — модель L2CS-Net (ResNet50) определяет углы pitch и yaw направления взгляда
- 🎯 **6 упражнений** — вверх-вниз, влево-вправо, две диагонали, круг по часовой и против часовой стрелки
- ✅ **Автоматический контроль** — упражнение засчитывается только при удержании взгляда на цели в течение 1 секунды
- 📊 **Визуальная обратная связь** — стрелка направления взгляда, индикатор прогресса, целевые точки
- 🔌 **Модульная архитектура** — спроектирован как модуль, пригодный для встраивания в любое стороннее приложение

---

## 🏗️ Структура проекта

```
├── eye_exercise.py          # Основная программа гимнастики для глаз
├── l2cs/                    # Библиотека L2CS-Net
│   ├── __init__.py
│   ├── model.py             # Архитектура нейронной сети L2CS (ResNet50)
│   ├── pipeline.py          # Конвейер: детекция лица → предсказание взгляда
│   ├── vis.py               # Визуализация направления взгляда
│   ├── results.py           # Контейнер результатов (pitch, yaw, bboxes)
│   ├── datasets.py          # Загрузка датасетов (Gaze360, MPIIGaze)
│   └── utils.py             # Вспомогательные функции
├── pyproject.toml           # Конфигурация проекта и зависимости
├── models/                  # Предобученные веса модели (не в репозитории)
│   └── L2CSNet_gaze360.pkl
└── README.md
```

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/<ваш-username>/angry-eye-calculation.git
cd angry-eye-calculation
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv
source .venv/bin/activate        # macOS / Linux
# .venv\Scripts\activate         # Windows
```

### 3. Установка зависимостей

```bash
pip install -e .
```

Это установит все необходимые библиотеки: PyTorch, OpenCV, NumPy, face_detection (RetinaFace) и другие.

### 4. Скачивание предобученной модели

Скачайте файл весов модели `L2CSNet_gaze360.pkl` из [Google Drive](https://drive.google.com/drive/folders/17p6ORr-JQJcw-eYtG2WGNiuS_qVKwdWd?usp=sharing) и поместите его в папку `models/`:

```bash
mkdir -p models
# Поместите скачанный файл L2CSNet_gaze360.pkl в папку models/
```

### 5. Запуск программы

```bash
python eye_exercise.py
```

---

## 🎮 Как пользоваться

1. **Запустите программу** — откроется окно с видеопотоком с камеры
2. **Расположитесь перед камерой** на расстоянии 40–80 см, лицо должно быть хорошо видно
3. **Следуйте за целевыми точками** — на экране отображаются жёлтые кружки, на которые нужно направить взгляд
4. **Удерживайте взгляд** на текущей цели — когда программа определит, что вы смотрите в правильном направлении, начнёт заполняться индикатор прогресса (синий сектор)
5. **После удержания 1 секунду** цель засчитывается и программа переходит к следующей точке
6. **Выполните все 6 упражнений** — после завершения появится поздравительное сообщение

### Управление

| Клавиша | Действие |
|---------|----------|
| `q` | Выход из программы |
| `s` | Пропустить текущее упражнение |

### Упражнения

| № | Название | Описание | Повторений |
|---|----------|----------|------------|
| 1 | Вверх-вниз | Перевод взгляда от верхнего края к нижнему | 3 |
| 2 | Влево-вправо | Перевод взгляда от левого края к правому | 3 |
| 3 | Диагональ ↗↙ | Из левого верхнего угла в правый нижний | 3 |
| 4 | Диагональ ↘↖ | Из левого нижнего угла в правый верхний | 3 |
| 5 | Круг ↻ | Обход 8 точек по часовой стрелке | 1 |
| 6 | Круг ↺ | Обход 8 точек против часовой стрелки | 1 |

---

## 🔧 Как это работает

### Конвейер обработки

```
Веб-камера (640×480, 30 FPS)
       ↓
RetinaFace — детекция лица
       ↓
Обрезка и масштабирование (224×224)
       ↓
L2CS-Net (ResNet50) — предсказание pitch и yaw
       ↓
Преобразование углов в экранные координаты
       ↓
Проверка совпадения направления взгляда с целью
       ↓
Визуализация и обратная связь
```

### Алгоритм проверки взгляда

Направление взгляда (pitch, yaw) преобразуется в вектор на экране:

```
dx = −sin(pitch) · cos(yaw)
dy = −sin(yaw)
```

Затем вычисляется угол между вектором взгляда и вектором к целевой точке. Если угол менее 30° — взгляд считается направленным на цель.

---

## 📋 Требования

- Python 3.8+
- Веб-камера (встроенная или внешняя)
- PyTorch 1.10+
- OpenCV 4.5+
- CUDA-совместимая видеокарта (опционально, для ускорения)

---

## 🔮 Планы развития

- Разработка открытого API для удобного встраивания модуля в сторонние приложения
- Публикация в виде pip-пакета
- Веб-интерфейс для работы через браузер
- Пилотное внедрение в образовательные платформы и системы тестирования

---

## 📄 Лицензия

Проект основан на [L2CS-Net](https://github.com/Ahmednull/L2CS-Net). См. файл [LICENSE](LICENSE).

